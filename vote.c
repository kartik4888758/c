#include <stdio.h>
int main(){
    int a;
    printf("enter your age",a);
    scanf("%d",&a);
    
    if(a=18 && a>18){
        printf("you are eligible to vote");
    }
    else
    {
        printf("not eligible to vote");
    }
    return 0;
}