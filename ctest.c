#include <stdio.h>
int main(){

    FILE *fptr;
    fptr=fopen("test.txt","r");

    // char ab;
    // ab=fgetc(fptr);

    // while (ab!=EOF)
    // {
    //     printf("%c",ab);
    //     ab=fgetc(fptr);
    // }
    // fclose(fptr);
    // return 0;
}
